import styles from '../styles/footer.module.css';

export default function Header(){
     return (
          <footer className={styles.footercontainer}>
               <span>Pen and Table</span>
          </footer>
     );
}